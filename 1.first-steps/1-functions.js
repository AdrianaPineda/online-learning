var name = 'Adriana', age = 27
function printAge() {
  console.log(`${name} is ${age} years old`)
}

printAge()

function printAgeWithParams(name, age) {
  console.log(`${name} is ${age} years old`)
}

printAgeWithParams('A', 2)

console.log(window.name)
